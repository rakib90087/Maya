package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Cosmic Dark Palette
val DeepSlate = Color(0xFF090D16)      // Dark background
val SpaceBlue = Color(0xFF131B2E)      // Surface cards
val NeonCyan = Color(0xFF00E5FF)       // Primary branding
val ElectricIndigo = Color(0xFF6366F1) // Secondary
val SoftSlate = Color(0xFF1E293B)      // Secondary background
val AquaGreen = Color(0xFF10B981)      // Completed state / green indicator
val AmberGold = Color(0xFFF59E0B)      // Warning / pending
val SunsetRed = Color(0xFFEF4444)      // Delete / error state

// Cream Palette (Cream Light Theme)
val CreamBg = Color(0xFFFAF6EE)        // Rich warm cream background
val CreamSurface = Color(0xFFFFFDF9)   // Bright warm cream for card surfaces
val SoftCreamVariant = Color(0xFFEFEAD8) // Soft beige for borders/dividers/variants
val CreamPrimary = Color(0xFF8C6D58)   // Warm terracotta brown / earth tone
val CreamSecondary = Color(0xFF5C7F67) // Sage green
val CharcoalText = Color(0xFF332F2C)   // Warm deep dark brown/charcoal

// Espresso Palette (Dark Companion Theme)
val EspressoBg = Color(0xFF1E1B18)       // Deep espresso dark brown background
val EspressoSurface = Color(0xFF2B2622)  // Soft warm dark brown for cards
val EspressoPrimary = Color(0xFFD9C3B0)  // Warm cream/beige accent
val EspressoSecondary = Color(0xFFA2B59F)// Muted sage green
val LatteText = Color(0xFFFDFBF7)        // Cream white

// Light palette placeholders
val LightBlueBg = Color(0xFFFAF6EE)
val LightSurface = Color(0xFFFFFDF9)
val MutedSlate = Color(0xFF6E685E)
val DarkNavy = Color(0xFF332F2C)

